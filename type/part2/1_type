let a = "hello"
// a = 1

let b : boolean = false
// : boolean -> 타입을 추론하지 못할 때. 명시적 표현. 최소한으로 사용하는 것이 좋음

let c = [1, 2, 3]
// c.push("1")

const player = {
    name: "nico"
}
// player.hello()
// player.name = 1

